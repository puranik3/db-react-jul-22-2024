// +, -, *, /, %, ** (exponentiation)

// !, &&, ||

// <, >, <=, >=, !=, ==

// ===, !==
console.log(1 == '1'); // true

// prefer using === rather than ==
console.log(1 === '1'); // false -> different data types

// condition ? on_true : on_false